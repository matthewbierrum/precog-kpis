# Neon Postgres — copy the connection string from the Neon console.
# Only needed once mock data in lib/kpis.ts is replaced with real SQL.
DATABASE_URL=postgresql://user:password@host/dbname?sslmode=require
